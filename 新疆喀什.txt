shilt + alt  +p

	avclink指令集说明书




通讯方式:
	TCP,UDP

端口地址:
	支持混插:
		地址位以A1开头,	
		8u
			双纤端口
			双纤端口地址:
				槽位  A101 (槽位1)
				端口  BO1  (端口1)
				输入/输出   (C01)
					
			单纤端口
				输入:C01
				输出:C70
				单纤端口输入地址:
					槽位  A101 (槽位1)
					端口  BO1  (端口1)
					输入   	 (C01)
				单纤端口输出地址:
					槽位  A101 (槽位1)
					端口  BO1  (端口1)
					输出   	 (C70)
	不支持混插
		地址位以A0开头	
		26u
设备地址:
	设备地址:	
		ADDR,,  默认的设备地址是0	
		当设备自己的地址是0时,该设备会接收所有的指令
		当发送指令集的设备地址是0时，设备自已的地址无论是什么，都要接收该指令。
		当设备自已的地址不是0，并且发送指令集的设备地址不是0，必须两者地址都匹配，设备才会接收该指令。
		例如：发送(INFO,0)，可以查询设备地址
		({"ctrlAddr":"2","deviceScale":8,"inputCardNum":0,"outputCardNum":20,"channelNum":8,"blend":true,"deviceName":"avclink"})
		ctrlAddr：2表示设备地址为2.



第三方指令集:
	指令的基本格式:
		(指令名，设备地址，数据，数据....)  全部要求带有设备地址,如果设备地址为0，默认为当前设备...
	
		单端口切换指令(增量)
			基本格式:(SWCH,ADDR,V/C,IN-OUT),V/C表示切换视频还是控制,如果是C,那么视频也一同切换
			返回值:操作正确返回字符(SWCH,ADDR.OK)
			说明:		
					此指令用于将某一个特定的输入端口切换到一路特定的输出端口。设备地址为可选项。
			例如:	
				将设备地址为0的端口1-1切换到1-2。
					实现指令： (SWCH,0,V,A101B01-A101B02)

				注意:此指令只会做增量替换,不会关闭端口.
	
		批量切换指令(增量)
			基本格式:
			返回值:
			说明:

		批量切换指令(覆盖)
			基本格式:
			返回值:
			说明:

		全端口切换
			基本格式:
			返回值:
			说明:

		关闭单个输出端口
			基本格式:
			返回值:
			说明:

		关闭多个输出端口
			基本格式:
			返回值:
			说明:

		输出端口切换状态查询
			基本格式:
			返回值:
			说明:
				注意:如果端口不存在,不会返回ERR或Warn,会返回其对应的切换状态为0
		保存场景

		
		获取场景列表
	
		调出场景
	
		修改设备地址

		查询设备的基本信息

		获取输入端口列表(v1.7)

		获取输出端口列表(v1.7)

		输入端口切换状态查询(v1.6)
		

错误码:


